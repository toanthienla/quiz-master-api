{
    "name": "quiz-master-api",
    "version": "1.0.0",
    "description": "Fake API for quiz app",
    "scripts": {
        "start": "json-server datas.json --host 0.0.0.0 --port $PORT"
    },
    "dependencies": {
        "json-server": "^0.17.3"
    }
}
